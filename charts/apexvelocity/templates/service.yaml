apiVersion: v1
kind: Service
metadata:
  name: {{ include "apexvelocity.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "apexvelocity.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}


